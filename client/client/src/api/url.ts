const url = "http://localhost:3000"

const urlGet = `${url}/getPokemon/`
const urlGetAll = `${url}/getPokemons`
const urlPost = `${url}/create-pokemon`
const urlDeleteAll = `${url}/deleteAll`

export { url, urlGet, urlGetAll, urlPost, urlDeleteAll }
